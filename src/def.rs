pub const NORMAL_LABOR_CARD: usize = 0;
pub const ALL_LABOR_CARD: usize = 1;
pub const CANCEL_CARD: usize = 2;
pub const ALL_CANCEL_CARD: usize = 3;
pub const CAPITAL_INCREASE_CARD: usize = 4;

pub struct Input {
    pub n: usize,
    pub m: usize,
    pub k: usize,
    pub t: usize,
}

pub struct State {}
